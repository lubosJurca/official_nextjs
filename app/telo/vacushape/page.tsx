import DetailPage from '@/components/detail-page/DetailPage';
import { vacushapeData } from '@/lib/data';

const VacuShape = () => {
  return <DetailPage {...vacushapeData} />;
};

export default VacuShape;
